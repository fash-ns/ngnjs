import {NGNApiRequest} from "ngn/dist/types/NGNApiRequest";
import {NGNApiResponse} from "ngn/dist/types/NGNApiResponse";
import {NextHandler} from "ngn/dist/types/NextHandler";
import {Middleware} from "ngn/dist/types/Middleware";

const {name}: Middleware = (req: NGNApiRequest, res: NGNApiResponse, next: NextHandler) => {
    next(); //TODO: implement {name} middleware
}

export default {name};