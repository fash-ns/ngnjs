import withSetCookie from "ngn/dist/middlewares/withSetCookie";
import decryptCookies from "ngn/dist/middlewares/decryptCookies";
import Keyable from "ngn/dist/types/Keyable";
import {Middleware} from "ngn/dist/types/Middleware";
import withRequest from "ngn/dist/middlewares/withRequest";
import withValidator from "ngn/dist/middlewares/withValidator";
import withIp from "ngn/dist/middlewares/withIp";
import withThrottle from "ngn/dist/middlewares/withThrottle";
import withSession from "ngn/dist/middlewares/withSession";

const middlewares: Keyable<Array<Middleware>> = {
    "/": [
        withRequest,
        withIp,
        withThrottle,
        decryptCookies,
        withSetCookie,
        withValidator,
        withSession
    ]
};

export default middlewares;